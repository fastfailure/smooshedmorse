/// --.---.---.-- is one of five 13-character sequences that does not appear in the
/// encoding of any word. Find the other four.
pub fn run() -> Result<Vec<String>, &'static str> {
    Ok(vec!["todo".to_string()])
}
